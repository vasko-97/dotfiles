.shell pwsh

# Note that the combo ctrl+alt is a way to map AltGr/Right Alt.

# Reload whkd configuration
# alt + o                 : taskkill /f /im whkd.exe && start /b whkd # if shell is cmd
alt + o                 : taskkill /f /im whkd.exe; Start-Process whkd -WindowStyle hidden # if shell is pwsh / powershell
alt + shift + o         : komorebic reload-configuration

alt + i                 : komorebic toggle-shortcuts

# App shortcuts - these require shell to be pwsh / powershell
# The apps will be focused if open, or launched if not open
# alt + f                 : if ($wshell.AppActivate('Firefox') -eq $False) { start firefox }
# alt + b                 : if ($wshell.AppActivate('Chrome') -eq $False) { start chrome }

ctrl + alt + q                 : komorebic close
alt + m                        : komorebic minimize

# Focus windows
alt + l                 : komorebic focus right
alt + h                 : komorebic focus left
alt + k                 : komorebic cycle-focus previous
alt + j                 : komorebic cycle-focus next

# Move windows
alt + shift + h         : komorebic move left
alt + shift + l         : komorebic move right
alt + shift + return    : komorebic promote

# Stack windows
alt + left              : komorebic stack left
alt + down              : komorebic stack down
alt + up                : komorebic stack up
alt + right             : komorebic stack right
alt + oem_1             : komorebic unstack # oem_1 is ;
alt + oem_4             : komorebic cycle-stack previous # oem_4 is [
alt + oem_6             : komorebic cycle-stack next # oem_6 is ]

# Resize
alt + oem_plus          : komorebic resize-axis horizontal increase
alt + oem_minus         : komorebic resize-axis horizontal decrease
alt + shift + oem_plus  : komorebic resize-axis vertical increase
alt + shift + oem_minus : komorebic resize-axis vertical decrease

# Manipulate windows
ctrl + alt + t                 : komorebic toggle-float # v-todo: consider reusing this mapping for something more useful
ctrl + alt + f                 : komorebic toggle-monocle

# Window manager options
ctrl + alt + r          : komorebic retile
alt + p                 : komorebic toggle-pause

# Layouts
ctrl + alt + x                 : komorebic focus-last-workspace
ctrl + alt + y                 : komorebic move-to-workspace 6 # a more ergonomic mappings to move stuff to the "catch-all" workspace, which is workspace 6

# Workspaces
ctrl + alt + 1                 : komorebic focus-workspace 0
ctrl + alt + 2                 : komorebic focus-workspace 1
ctrl + alt + 3                 : komorebic focus-workspace 2
ctrl + alt + 4                 : komorebic focus-workspace 3
ctrl + alt + 5                 : komorebic focus-workspace 4
alt + 6                        : komorebic focus-workspace 5
alt + 7                        : komorebic focus-workspace 6
alt + 8                        : komorebic focus-workspace 7
ctrl + alt + s                 : komorebic cycle-workspace next
ctrl + alt + a                 : komorebic cycle-workspace previous

# Move windows across workspaces
alt + shift + 1         : komorebic move-to-workspace 0
alt + shift + 2         : komorebic move-to-workspace 1
alt + shift + 3         : komorebic move-to-workspace 2
alt + shift + 4         : komorebic move-to-workspace 3
alt + shift + 5         : komorebic move-to-workspace 4
alt + shift + 6         : komorebic move-to-workspace 5
alt + shift + 7         : komorebic move-to-workspace 6
alt + shift + 8         : komorebic move-to-workspace 7

# v-todo: add `alt + shift + s` to move window to the workspace to the right etc.
# v-todo: e.g. win + 1                 : komorebic eager-focus firefox.exe -- https://lgug2z.github.io/komorebi/usage/focusing-windows.html
# v-todo: e.g. alt + return            : komorebic promote-focus           -- https://lgug2z.github.io/komorebi/usage/focusing-windows.html
# v-todo: use AHK where needed to avoid app keystroke conflicts.. e.g. Git Extension overrides Alt+Y with no ability to disable it
# v-todo: create binding or script to start komorebi and then after komorebi has successfully started run FocusOpenWindows.ps1